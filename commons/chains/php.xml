<?xml version="1.0" encoding="UTF-8"?>


<project>

    <echo message="loading chain: php" />

    <target name="init:main" hidden="true" depends="composer.validate, composer.update" />
    <target name="test:main" hidden="true" depends="test.phplint, test.phpunit" />
    <target name="build:before" hidden="true" depends="project.jenkins.update" />
    <target name="build:after" hidden="true" depends="project.jenkins.update" />

    <!-- additional targets -->
    <target name="release" hidden="true" depends="test, project.version.update, composer.format, project.jenkins.update" />

</project>